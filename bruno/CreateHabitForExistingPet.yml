info:
  name: CreateHabitForExistingPet
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:9812/pets/1/habits
  body:
    type: json
    data: |-
      {
        "name": "Workout",
        "category": "health",
        "targetFrequency": 5,
        "statBoost": "happiness"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
