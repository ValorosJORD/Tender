info:
  name: GetPetsHappinessFiltered
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:9812/pets?minHappiness=50
  params:
    - name: minHappiness
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
