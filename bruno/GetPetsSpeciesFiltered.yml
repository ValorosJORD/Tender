info:
  name: GetPetsSpeciesFiltered
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:9812/pets?species=dragon
  params:
    - name: species
      value: dragon
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
