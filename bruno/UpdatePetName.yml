info:
  name: UpdatePetName
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:9812/pets/1
  body:
    type: json
    data: |-
      {
        "name": "Johnson"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
